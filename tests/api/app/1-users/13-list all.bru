get {
    url: {{baseUrl}}/app/users
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all users at the end should at least find admin users", function() {
        expect(res.body.every(u => u.tier === 0)).to.be.true;
    })
}