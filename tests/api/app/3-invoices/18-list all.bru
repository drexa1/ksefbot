get {
    url: {{baseUrl}}/app/invoices
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all invoices at the end should find nothing", function() {
        expect(res.status).to.equal(404);
        expect(res.body).to.have.property("error", "No invoice found");
    })
}