get {
    url: {{baseUrl}}/app/contractors?nip={{salesInvoiceCustomerId}}
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List the recently created buyer contractor", function() {
        console.info(res.body);
        expect(res.body[0].nip).to.equal(bru.getCollectionVar("salesInvoiceCustomerId"));
    })
}