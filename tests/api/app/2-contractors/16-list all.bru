get {
    url: {{baseUrl}}/app/contractors
}
headers {
    Content-Type: application/json
    X-API-Key: {{apiKey}}
}
tests {
    test("List all contractors at the end should fail", function() {
        expect(res.status).to.equal(404);
    })
}