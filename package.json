{
    "name": "ksefbot",
    "version": "1.0.0",
    "description": "Invoicing automation for KSeF",
    "keywords": [
        "KSeF",
        "Invoicing",
        "Automation",
        "Poland"
    ],
    "homepage": "https://github.com/drexa1/ksef-bot#readme",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/drexa1/ksef-bot.git"
    },
    "author": "drexa1",
    "scripts": {
        "db:dev:teardown": "wrangler d1 execute ksef-bot --config wrangler.worker-ksef.toml --local --file=db/teardown.sql",
        "db:dev:create": "wrangler d1 execute ksef-bot --config wrangler.worker-ksef.toml --local --file=db/schema.sql",
        "db:dev:populate": "wrangler d1 execute ksef-bot --config wrangler.worker-ksef.toml --local --file=db/populate.sql",
        "db:dev:recreate": "npm run db:dev:teardown && npm run db:dev:create && npm run db:dev:populate",
        "⚠ db:prod:teardown": "wrangler d1 execute ksef-bot --remote --file=db/teardown.sql",
        "⚠ db:prod:create": "wrangler d1 execute ksef-bot  --remote --file=db/schema.sql",
        "⚠ db:prod:populate": "wrangler d1 execute ksef-bot  --remote --file=db/populate.sql",

        "api:tests:wait": "wait-on http://localhost:3001 && npm run api:tests:bruno",
        "api:tests:bruno": "cd tests/api && bru run -r --env dev --sandbox dev",
        "api:dev:tests": "npm run db:dev:recreate && concurrently -k npm:worker-api:dev:run npm:api:tests:wait",
        "api:ci:tests": "bash .github/scripts/api-tests-ci.sh",

        "client:dev:build": "vite build --mode development",
        "client:dev:run": "npm run client:dev:build && vite --port 3000",
        "client:dev:test": "vitest",
        "client:prod:build": "vite build --mode production",

        "worker-api:dev:run": "npm run client:dev:build && wrangler dev --config wrangler.worker-ksef.toml --local --port 3001 --inspector-port 9999",
        "worker-api:prod:deploy": "npm run client:prod:build && wrangler deploy --config wrangler.worker-ksef.toml",
        "worker-api:prod:logs": "wrangler tail ksefbot",

        "worker-whatsapp:dev:run": "wrangler dev --config wrangler.worker-whatsapp.toml --local --port 3002 --inspector-port 9999",
        "worker-whatsapp:prod:deploy": "wrangler deploy --config wrangler.worker-whatsapp.toml"
    },
    "dependencies": {
        "@sentry/cli": "latest",
        "asn1js": "latest",
        "concurrently": "latest",
        "fast-xml-builder": "latest",
        "fast-xml-parser": "latest",
        "p-retry": "latest",
        "pkijs": "latest",
        "vite": "latest"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "latest",
        "@sentry/cloudflare": "latest",
        "@sentry/tracing": "latest",
        "@types/bootstrap": "latest",
        "@usebruno/cli": "latest",
        "glob": "latest",
        "typescript": "latest",
        "vitest": "latest",
        "wait-on": "latest",
        "wasm-pack": "latest",
        "wrangler": "latest"
    }
}
